apiVersion: v1
kind: Service
metadata:
  name: jenkins-service
  namespace: devops-tools
spec:
  type: NodePort
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
  selector:
    app: jenkins

# ==============
# apiVersion: v1
# kind: Service
# metadata:
#   name: jenkins-service
#   namespace: devops-tools
# spec:
#   selector:
#     app: jenkins
#   type: LoadBalancer  # Exposes Jenkins on an external IP
#   ports:
#     - port: 8080
#       targetPort: 8080
